"""Shared pytest fixtures for the whole suite live here.

conftest.py is auto-discovered by pytest — no import needed. Define fixtures with
@pytest.fixture and any test in this directory (or below) can request them by name.
Empty for now; add fixtures as the project grows.
"""
