"""A placeholder test so the suite isn't empty (pytest fails a run that collects nothing).

Replace with your project's real tests.
"""

import project_template


def test_package_imports():
    assert project_template.__name__ == "project_template"
